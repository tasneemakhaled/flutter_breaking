import 'package:flutter/material.dart';

class CharchtersScreen extends StatelessWidget {
  const CharchtersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
